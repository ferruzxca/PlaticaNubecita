# In all environments, the following files are loaded if they exist,
# the latter taking precedence over the former:
#
#  * .env                contains default values for the environment variables needed by the app
#  * .env.local          uncommitted file with local overrides
#  * .env.$APP_ENV       committed environment-specific defaults
#  * .env.$APP_ENV.local uncommitted environment-specific overrides
#
# Real environment variables win over .env files.

APP_ENV=dev
APP_SECRET=change-this-in-production
APP_BASE_URL=http://localhost
APP_SHARE_DIR=var/share

DEFAULT_URI=http://localhost

# Hostinger MySQL sample:
# DATABASE_URL="mysql://u123456789_user:password@localhost:3306/u123456789_chat?serverVersion=10.11.2-MariaDB&charset=utf8mb4"
DATABASE_URL="sqlite:///%kernel.project_dir%/var/data_%kernel.environment%.db"

MAILER_DSN=null://null
MAILER_FROM=no-reply@platica-nubecita.local

# 32-byte base64 key (replace in production)
APP_ENCRYPTION_KEY=A17VsIHEyxNTtuly3ken+U4min4XKTIpbELQOfFy9bM=
APP_ENCRYPTION_KEY_VERSION=1

# Random secret used to hash one-time tokens (replace in production)
APP_TOKEN_HASH_KEY=ae03fca715f88642185b25765a8f2a32255498c00932525a875d8cd00823a49d
APP_MAX_UPLOAD_BYTES=26214400
